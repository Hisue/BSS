using System.Threading.Tasks;
using BSSUnitTest.Orders;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace UnitTestProject1
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        //public void GetAvailableIdAsync_OrderNotFoundInRepository_ShouldReturnAvailableIdTrue()
        public void TestMethod1()
        {
            ////Arrange
            //var orderRepository = new OrderRepository();
            //var orderService = new OrderService(orderRepository);
            //const string testOrderId = "5";

            ////Act
            //var availableId =  orderService.GetAvailableIdAsync(testOrderId);

            ////Assert
            //Assert.IsTrue(availableId);
        }
    }
}
